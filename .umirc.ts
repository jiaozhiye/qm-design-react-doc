import { defineConfig } from 'dumi';

export default defineConfig({
  title: '技术平台文档',
  mode: 'site',
  locales: [['zh-CN', '中文']],
  favicon: '/favicon.ico',
  logo: '/assets/img/logo.png',
  publicPath: '/',
});
